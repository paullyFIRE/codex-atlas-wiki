---
import BaseLayout from '../../layouts/BaseLayout.astro';

const files = import.meta.glob('/data/pages/modes/_index.json', { eager: true, import: 'default' });
const index = files['/data/pages/modes/_index.json'] ?? { items: [], count: 0 };
const items = index.items ?? [];
---

<BaseLayout
  title="Game Modes - Rules, Rewards, and Strategy | War Inc: Rising Wiki"
  description={`Learn about all ${items.length} game modes in War Inc: Rising.`}
  currentSection="guides"
>
  <nav class="flex items-center gap-2 text-label-md font-bold text-on-surface-variant mb-8 uppercase tracking-wider">
    <a href="/" class="hover:text-primary no-underline">Home</a>
    <span class="material-symbols-outlined text-xs">chevron_right</span>
    <span class="text-primary bg-primary-container/20 px-3 py-1 rounded-full">Game Modes</span>
  </nav>
  <h1 class="font-display text-headline-lg text-on-surface mb-4 leading-none">Game Modes</h1>
  <p class="text-body-md text-on-surface-variant mb-8">{items.length} modes with different rules, enemy pools, and rewards.</p>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    {items.map((item) => (
      <a href={`/modes/${item.slug}/`} class="bg-white border-4 border-surface-container rounded-xl p-6 clash-card hover:border-primary transition-all group no-underline hover:-translate-y-1 block">
        <h2 class="text-title-md font-display text-on-surface group-hover:text-primary transition-colors">{item.name}</h2>
      </a>
    ))}
  </div>
</BaseLayout>
