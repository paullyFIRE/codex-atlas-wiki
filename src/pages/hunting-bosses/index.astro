---
import BaseLayout from '../../layouts/BaseLayout.astro';

const files = import.meta.glob('/data/pages/hunting-bosses/_index.json', { eager: true, import: 'default' });
const index = files['/data/pages/hunting-bosses/_index.json'] ?? { items: [], count: 0 };
const items = index.items ?? [];
---

<BaseLayout
  title="All Hunting Mode Bosses | War Inc: Rising Wiki"
  description={`Browse all ${items.length} Hunting mode bosses in War Inc: Rising. Stats, abilities, and strategies.`}
>
  <nav class="flex items-center gap-2 text-label-md font-bold text-on-surface-variant mb-8 uppercase tracking-wider">
    <a href="/" class="hover:text-primary no-underline">Home</a>
    <span class="material-symbols-outlined text-xs">chevron_right</span>
    <span class="text-primary bg-primary-container/20 px-3 py-1 rounded-full">Hunting Mode Bosses</span>
  </nav>
  <h1 class="font-display text-headline-lg text-on-surface mb-4 leading-none">All Hunting Mode Bosses</h1>
  <p class="text-body-md text-on-surface-variant mb-8">{items.length} boss enemies encountered in Hunting mode.</p>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    {items.sort((a, b) => b.combat_power - a.combat_power).map((item) => (
      <a href={`/hunting-bosses/${item.slug}/`} class="bg-white border-4 border-surface-container rounded-xl p-6 clash-card hover:border-primary transition-all group no-underline hover:-translate-y-1 block">
        <h2 class="text-title-md font-display text-on-surface group-hover:text-primary transition-colors">{item.name}</h2>
      </a>
    ))}
  </div>
</BaseLayout>
